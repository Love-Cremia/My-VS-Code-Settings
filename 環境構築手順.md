# 環境構築手順

## 本リポジトリをクローンできる場合

実施項目は以下：

- 各種拡張機能のインストール
- ホットキーの割り当て
- ユーザ設定の適用
- プロジェクトに設定ファイルを配置
- プロジェクトに textlint の適用

## 拡張機能のインストール

[README](./README.md) にあるものをインストールする．

## ホットキーの割り当て

| 機能                      | 設定値           | 備考                          |
| ------------------------- | ---------------- | ----------------------------- |
| Markdown Preview 横に表示 | Ctrl + Shift + V | デフォルトは Ctrl + K, V      |
| Markdown Preview 切替     | なし             | デフォルトは Ctrl + Shift + V |

## ユーザ設定の適用

Mac の場合は `sync-vscode-settings.command` を，Windows の場合は
`sync-vscode-settings.bat` を実行する．

## プロジェクトに設定ファイルを配置

`ProjectSettings` フォルダの中身を一式プロジェクトルートにコピーする．
ただしコピーしないほうがいい場合もあるので注意．

### markdownlint 設定 JSON

`.markdownlint.jsonc` ファイルがプロジェクトルートにある時点で，ユーザ用
settings.json による設定は無視されることになる．

### textlint 設定ファイル

- `.textlintrc.json`: 後述の textlint
    のインストールを行わないのであればあってもなくても変化は起きないが，余計な設定ファイルがリポジトリに残ると混乱のもとなので注意する．
- `package.json`, `package-lock.json`: 後述の textlint インストール時に用いる．どんなパッケージを用いるのかをメンバー間で共有するため，
    textlint を使用するならこの 2 ファイルは必ず Git に登録すること．

## プロジェクトに textlint をインストール
