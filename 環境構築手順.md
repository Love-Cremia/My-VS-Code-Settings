# 環境構築手順

実施項目は以下：

- 各種拡張機能のインストール
- ホットキーの割り当て
- ユーザ設定の適用
- プロジェクトに設定ファイルを配置
- プロジェクトに textlint の適用

## 拡張機能のインストール

[README](./README.md) にあるものをインストールする．

## ホットキーの割り当て

| 機能                      | 設定値           | 備考                          |
| ------------------------- | ---------------- | ----------------------------- |
| Markdown Preview 横に表示 | Ctrl + Shift + V | デフォルトは Ctrl + K, V      |
| Markdown Preview 切替     | なし             | デフォルトは Ctrl + Shift + V |

## ユーザ設定の適用

Mac の場合は `sync-vscode-settings.command` を，Windows の場合は
`sync-vscode-settings.bat` を実行し，`2` を入力する．これにより
`UserSettings` フォルダの中身が適切な場所に配置される．

## プロジェクト設定

`ProjectSettings` フォルダの中身を**適宜**プロジェクトルートにコピーする．
コピーしないほうが良い場合もあるので注意．

### markdownlint 設定 JSON

プロジェクトで統一した markdownlint の設定を行う場合は，`.markdownlint.jsonc` を配置する．
このファイルがプロジェクトルートにある時点で，ユーザ用 settings.json による設定は無視されることになる．

### textlint 環境構築

textlint は Node.js の思想 (再現性と閉塞性) から，必ずプロジェクトにインストールする．
ホームディレクトリ/ユーザプロファイル下や VS Code のユーザフォルダ配下にインストールしてグローバルに適用しようとすると不具合が起きやすい
(特に Windows の場合)．

textlint を使う場合は，まず以下の 3 ファイルをプロジェクトルートに配置する．使わない場合は混乱を避けるため配置しないこと．

- `.textlintrc.json`
- `package.json`
- `package-lock.json`

<!-- 後述の textlint
    のインストールを行わないのであればあってもなくても変化は起きないが，余計な設定ファイルがリポジトリに残ると混乱のもとなので注意する． -->
<!--     
: 後述の textlint インストール時に用いる．どんなパッケージを用いるのかをメンバー間で共有するため，
    textlint を使用するならこの 2 ファイルは必ず Git に登録すること． -->
#### Node.js

事前準備として，Node.js をインストールしておく必要がある．

Mac の場合は Homebrew で，Windows の場合は LTS のインストーラを使用する．

以下のコマンドが通れば OK：

```shell
node --version
npm --version
```

#### textlint インストール

プロジェクトルートで以下のコマンドを実行する：

```shell
npm ci
```

これにより `node_modules` というフォルダができ，そこに textlint がインストールされる．

##### 設定ファイル・固定ファイルがない場合

既存の `package.json`, `package-lock.json` が利用できない場合，あるいはこれと異なる設定をしたい場合は，以下のコマンドを実行する：

```shell
npm init -y
npm install --save-dev textlint textlint-rule-preset-ja-spacing
# 必要に応じて別のパッケージをインストールする
```

#### gitignore 登録

`node_modules` は必ず `.gitignore` に登録する．
5000 個以上のファイルが存在し管理対象にすべきでない．
代わりに `package.json`, `package-lock.json` を Git 管理することで，どの作業者も同じ環境を構築できる．
